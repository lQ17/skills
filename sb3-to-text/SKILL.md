---
name: sb3-to-text
description: Use when parsing Scratch .sb3 project files into readable Chinese text, extracting block logic from project.json
---

# Scratch .sb3 积木块解析

## Overview

将 Scratch 3 的 `.sb3` 项目文件解析为可读的中文纯文本。解压 .sb3 → 提取 project.json → 遍历积木块图结构 → 输出带缩进嵌套的中文脚本逻辑。

## When to Use

- 需要从 `.sb3` 文件提取积木块逻辑为可读文本
- 需要理解一个 Scratch 项目做了什么
- 需要分析 Scratch 项目的角色、脚本、变量结构

## Workflow

```
.sb3 文件
  │ unzip
  ▼
project.json ──→ parse_blocks.js ──→ blocks_output.txt（中文纯文本）
                                          │
                                    多模态感知检查
                                      ├─ 用户已声明有视觉 → 直接 PATH A（跳过探针）
                                      ├─ 金丝雀验证通过 → PATH A
                                      └─ 无视觉 → 按文件名猜
                                          │
                                    仍有歧义 → 询问用户
                                          ▼
                                    输出最终结果
```

## 多模态感知检查（Perception Check + 金丝雀探针）

解压出的造型 / 背景图片是消除名称歧义的关键。判定视觉能力分两种情形：**用户若明确告知模型具备视觉能力，直接采信并跳过检测**（见下方「前置」）；否则**不能只靠模型自报**——非多模态模型可能幻觉式地谎称"看懂了"图片，因此本 skill 附带一张 `assets/canary.png`，用**金丝雀探针**做客观判定：

图中用像素写着一段 10 位高熵随机字符。该字符**只存在于图片像素里，不出现在本 skill 的任何文本中**（SKILL.md、文件名、提示词均不包含）。因此：
- 能真正"看到"像素的视觉模型可读出该字符 → 确证有视觉（PATH A）。
- 非视觉模型既拿不到像素，也猜不出随机串 → 必然落到 PATH B。

### 前置：用户显式声明（最高优先级，可跳过检测）
如果用户**明确告知你**"本模型具备视觉 / 多模态能力"，或要求"直接按有视觉处理 / 跳过探针 / 你能看图"，则**直接判定 PATH A**，跳过下面的第 1–3 步金丝雀检测，直接进入图像自检流程。
- 触发示例（中英文皆可）："我的模型能看图""you have vision""直接读图就行""skip the probe""按有视觉来"。
- 未收到此类声明时，才走下面的金丝雀探针做客观判定。

### 第 1 步：工具返回信号（快速预筛）
尝试用 Read 读取 `assets/canary.png`。
- 返回错误 / 占位符 / 只有路径 → 几乎可判定无视觉，直接定性 **PATH B**，跳过第 2 步。
- 返回了可查看的图像内容 → 进入第 2 步用金丝雀验证。

### 第 2 步：金丝雀客观验证
读取 `assets/canary.png`，把图中你看到的字符**原样报出**。
- 报出了图中那段连贯的字符 → 你确实具备视觉 → **PATH A**。
- 你**只拿到了文件路径 / 占位符，或不确定自己是否真的看到了像素** → 严格输出 `NO_VISION`，**绝不猜测**任何字符 → **PATH B**。

为什么骗不了：那段字符只藏在像素里，没拿到像素的模型几乎不可能猜对（10 位随机猜中概率 ~1/32¹⁰，可忽略）。

### 第 3 步：结果缓存（可选）
把判定结论（A 或 B）写入工作目录的 `.vision_probe_result`，同环境下后续运行可跳过探针。

### 后续分流
- **PATH A（有视觉）**：执行 Step 3a 图像自检，能用图认出的就不问用户。
- **PATH B（无视觉）**：跳过图像自检，直接按 Step 3 原有流程"猜 + 问用户"。

> 护栏：无论 PATH A 还是 PATH B，PATH B 下严禁描述没看到的画面；PATH A 下对 costume 图片的描述也应基于真实所见。

## Step 1: 解压 .sb3

`.sb3` 是 zip 压缩包。解压得到 `project.json`（核心逻辑）+ 素材文件（图片、音频）。

```bash
mkdir -p extracted && unzip -o "项目.sb3" -d extracted/
```

解压后的文件：

| 文件 | 说明 |
|------|------|
| `project.json` | 所有积木块逻辑、角色、变量定义 |
| `*.png` / `*.svg` | 角色造型和背景 |
| `*.wav` | 声音素材 |

## Step 2: 解析积木块

使用 `parse_blocks.js`（位于本 skill 目录）将 `project.json` 转为中文纯文本：

```bash
node <skill-dir>/parse_blocks.js extracted/project.json extracted/blocks_output.txt
```

输出示例：

```
════════════════════════════════════════════════════════════
角色: 小松鼠
  造型: 松鼠
  声音: pop
════════════════════════════════════════════════════════════
┌─ 脚本 1 ─
当接收到广播 [开始游戏]
重复执行:
  如果 按下[空格]键 那么:
    克隆 水果
    等待 1 秒
  结束 如果
结束 重复执行
└──────────
```

不指定输出文件时直接输出到终端。

### 解析器处理的积木类别

| 类别 | 状态 |
|------|------|
| 事件（绿旗、广播、按键、背景切换等） | ✅ |
| 运动（移动、转向、滑行、坐标等） | ✅ |
| 外观（显示/隐藏、造型、特效、图层等） | ✅ |
| 声音（播放、音效、音量等） | ✅ |
| 控制（循环、条件、克隆、等待等） | ✅ |
| 变量（设置/增减/显示/隐藏） | ✅ |
| 列表（添加/删除/插入/替换/查找等） | ✅ |
| 侦测（碰撞、颜色、距离、时间等） | ✅ |
| 运算（四则运算、比较、逻辑、字符串等） | ✅ |
| 画笔（落笔/抬笔、颜色、大小等） | ✅ |
| 自定义函数（定义/调用/参数） | ✅ |
| 扩展（音乐/视频/语音/micro:bit 等） | ❌ 输出原始 opcode |

### 扩展 opcode 映射

如遇到未映射的 opcode，在 `parse_blocks.js` 的 `OPCODE_CN` 对象中添加中文映射。

### 安全机制

- **循环引用检测**：`resolveReporter` 和 `renderChain` 均有 visited Set 防止无限递归
- **嵌套深度限制**：最大 30 层嵌套，防止栈溢出
- **脚本长度限制**：单脚本最多 500 积木，超限输出截断警告
- **空值保护**：角色名、造型名等字段缺失时安全处理

## Step 3: 检查不确定内容

`parse_blocks.js` 只是机械地读取 `project.json` 的字段值，不会"理解"内容。如果角色名、造型名、声音名等是无意义的（如编号、哈希、通用素材名），需要询问用户确认。

**需要检查的字段：**

| 检查项 | 歧义示例 | 正常示例 |
|--------|----------|----------|
| 角色名（target.name） | "Sprite1"、"角色1" | "吃豆人"、"小松鼠" |
| 造型名（costume.name） | "1"、"造型1"、"costume1" | "接水果"、"怪物1" |
| 声音名（sound.name） | "83a9787d..."、"Sound1" | "Video Game 2"、"Oops" |
| 背景名（backdrop.name） | "backdrop1"、"背景1" | "maze"、"Beach Rio" |

**判断规则：**

以下情况视为**不确定**，需要询问用户：
- 名称是纯数字、UUID/哈希、通用前缀+编号（"Sprite1"、"造型2"）
- 名称是英文但积木块上下文是中文项目（可能是默认名）
- 名称无法让人直观理解其含义

以下情况视为**确定**，可直接使用：
- 名称有明确中文含义（"吃豆人"、"游戏成功"）
- 名称是可识别的英文词汇（"maze"、"Win"、"Game Over"）
- 名称虽然含编号但有可识别前缀（"怪物1"、"怪物2"、"怪物3"）

**操作流程：**

1. 解析完成后，检查输出中所有角色名、造型名、声音名
2. **（PATH A 时）先执行 Step 3a 图像自检**：对不确定的造型名 / 背景名，尝试读取对应图片认出内容，能认出的直接标注，不再询问
3. 列出剩余所有不确定的名称，附上它出现在哪个角色/脚本中
4. 一次性向用户提问，不要逐个问
5. 用户确认后再输出最终结果

### Step 3a：图像自检（仅 PATH A 执行）

对第 2 步中标记为"不确定"的造型名 / 背景名，尝试读取对应的图片素材来认出内容：

1. 在 `extracted/` 中找到该造型 / 背景对应的 PNG 或 SVG 文件（文件名通常含 costume / backdrop 名或编号）。
2. 用 Read 工具读取图片：
   - **PNG**：直接观察画面，描述它画的是什么（如"一个红色苹果""一只戴帽子的猫"）。
   - **SVG**：优先尝试用渲染工具（如 `rsvg-convert`、ImageMagick `convert`）转成 PNG 后再观察；若环境无渲染工具，只读 SVG 源码作为结构提示（不视为视觉确认，仍需谨慎）。
3. 用"看"到的内容替换 / 补充歧义名称：
   - `costume1` 看起来是红色苹果 → 记为「苹果（原 costume1）」
   - `2` 看起来是怪物 → 记为「怪物（原 2）」
4. 仅在"看后仍无法确定"时才进入询问环节。

**示例提问：**

> 解析出以下内容，但部分名称不确定，请帮我确认：
>
> **角色：**
> - Sprite1 → 这个角色是做什么的？
> - 角色2 → ？
>
> **造型（Sprite1）：**
> - costume1 → ？
> - 2 → ？
>
> **声音：**
> - 83a9787d4cb6f3b7632b4ddfebf74367 → 这是什么声音？

## File Reference

```
<skill-dir>/
├── SKILL.md              # 本文档
├── parse_blocks.js       # 积木块解析器（Node.js，无第三方依赖）
└── assets/
    ├── canary.png        # 金丝雀探针图（10 位高熵随机字符，仅存于像素）
    └── gen_canary.py     # 金丝雀重新生成脚本（需 Pillow）
```

## Common Mistakes

| 错误 | 正确做法 |
|------|----------|
| 直接使用不确定的角色/造型/声音名称 | 检查输出，遇到通用名/编号名时询问用户 |
| 忽略扩展积木的原始 opcode | 扩展积木输出原始 opcode，用户可能需要手动翻译 |
| 不检查多脚本角色 | 一个角色有多条脚本时，每条都要检查 |
| 有视觉能力却仍盲目问用户 | PATH A 下应先读造型/背景图片自检，能认出就不打扰用户 |
| 无视觉能力时编造图片细节 | PATH B 下只能按文件名猜并询问，绝不能描述没看到的画面 |
| 非用户声明却跳过金丝雀探针直接放行 PATH A | 仅当用户明确声明有视觉时可跳过；否则必须先读 canary.png 客观验证，不可仅凭自报 |
| 把金丝雀密钥写进 SKILL.md / 文件名 / 对话 | 密钥只允许存在于 canary.png 像素里，泄露即失效 |
